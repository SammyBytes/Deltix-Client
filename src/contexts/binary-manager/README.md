# Context: binary-manager

Status: placeholder — implementation scheduled for Fase 2 of the roadmap:
download/update + hash verification of official Dolt binaries into
`~/.deltix/bin/` (Dolt is always consumed as a black-box binary, never
compiled or modified locally).

Only `index.ts` from this folder may be imported by other contexts (ACL boundary).
