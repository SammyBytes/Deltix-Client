/**
 * Placeholder for the "binary-manager" bounded context.
 *
 * This is the ONLY file other contexts/modules are allowed to import from
 * (ACL boundary). Internals of this context must never be imported directly
 * from outside.
 *
 * Implementation lands in Fase 2 of the roadmap: download/update-verify
 * official Dolt binaries into `~/.deltix/bin/` (hash-verified, never
 * compiled/modified locally).
 */
export {};
